/// <reference types="vite/client" />

// Brings in Vite's ambient module declarations for asset imports
// (`*.png`, `*.svg`, …) so `tsc` type-checks `import logo from "./x.png"`
// the same way Vite resolves it at build time.
